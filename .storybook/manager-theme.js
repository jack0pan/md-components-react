import { create } from "@storybook/theming/create";

const baseTheme = {
  fontBase: '"Open Sans", sans-serif',
  brandTitle: "Material Design 3",
  brandUrl: "https://m3.material.io",
};

export default create(baseTheme);
